#include <stdio.h>
int main(){
    //Arithmatic operators
    
    // + = addition
    // - = subtraction
    // * = multiplication
    // / = division
    // % = modulus (remainder)

    int x = 5;
    int y = 2;

    //int z = x + y;
    //int z = x - y;
    //int z = x * y;
    //float z = x / (float) y;
    //int z = x % y;


    float z = x + y;
    printf("The sum of %d and %d is %.2f\n", x, y, z); 
    
    return 0;
}