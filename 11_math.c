#include <stdio.h>
#include <math.h>
int main (){
    double x = sqrt(16);
    double y = pow(2, 3);
    int c = round(6.7); //round is used to round to the nearest integer
    int d = ceil(6.7); //ceil is used to round up
    int e = floor(6.7); //floor is used to round down
    double f = fabs(-100); //fabs is used to get the absolute value
    double g = log(3.1416);
    double h = cos(45);
    double i = sin(45);
    double j = tan(45);

    printf("%lf", i);

    return 0;
}